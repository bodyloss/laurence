{
  "name": "laurance",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "start": "npm run makeDist && npm run buildIndex",
    "makeDist": "mkdir -p dist",
    "buildIndex": "./node_modules/.bin/handlebars settings.json < index.hbs > dist/index.html"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "handlebars-cmd": "^0.1.4",
    "normalize.css": "^7.0.0"
  }
}
